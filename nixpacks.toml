[variables]
NIXPACKS_NODE_VERSION = "20"

[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.npm", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm ci", "pip install -r requirements.txt"]

[start]
cmd = "node index.js"